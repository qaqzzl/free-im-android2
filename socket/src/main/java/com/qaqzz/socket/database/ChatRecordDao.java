package com.qaqzz.socket.database;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHAT_RECORD".
*/
public class ChatRecordDao extends AbstractDao<ChatRecord, Long> {

    public static final String TABLENAME = "CHAT_RECORD";

    /**
     * Properties of entity ChatRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Chatroom_id = new Property(1, String.class, "chatroom_id", false, "CHATROOM_ID");
        public final static Property Last_open_time = new Property(2, Long.class, "last_open_time", false, "LAST_OPEN_TIME");
        public final static Property Chat_type = new Property(3, String.class, "chat_type", false, "CHAT_TYPE");
        public final static Property Avatar = new Property(4, String.class, "avatar", false, "AVATAR");
        public final static Property Name = new Property(5, String.class, "name", false, "NAME");
        public final static Property Is_top = new Property(6, int.class, "is_top", false, "IS_TOP");
        public final static Property Deleted_at = new Property(7, Long.class, "deleted_at", false, "DELETED_AT");
        public final static Property Created_at = new Property(8, Long.class, "created_at", false, "CREATED_AT");
    }


    public ChatRecordDao(DaoConfig config) {
        super(config);
    }
    
    public ChatRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHAT_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"CHATROOM_ID\" TEXT NOT NULL ," + // 1: chatroom_id
                "\"LAST_OPEN_TIME\" INTEGER," + // 2: last_open_time
                "\"CHAT_TYPE\" TEXT NOT NULL ," + // 3: chat_type
                "\"AVATAR\" TEXT," + // 4: avatar
                "\"NAME\" TEXT," + // 5: name
                "\"IS_TOP\" INTEGER NOT NULL ," + // 6: is_top
                "\"DELETED_AT\" INTEGER," + // 7: deleted_at
                "\"CREATED_AT\" INTEGER);"); // 8: created_at
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHAT_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChatRecord entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
        stmt.bindString(2, entity.getChatroom_id());
 
        Long last_open_time = entity.getLast_open_time();
        if (last_open_time != null) {
            stmt.bindLong(3, last_open_time);
        }
        stmt.bindString(4, entity.getChat_type());
 
        String avatar = entity.getAvatar();
        if (avatar != null) {
            stmt.bindString(5, avatar);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(6, name);
        }
        stmt.bindLong(7, entity.getIs_top());
 
        Long deleted_at = entity.getDeleted_at();
        if (deleted_at != null) {
            stmt.bindLong(8, deleted_at);
        }
 
        Long created_at = entity.getCreated_at();
        if (created_at != null) {
            stmt.bindLong(9, created_at);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChatRecord entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
        stmt.bindString(2, entity.getChatroom_id());
 
        Long last_open_time = entity.getLast_open_time();
        if (last_open_time != null) {
            stmt.bindLong(3, last_open_time);
        }
        stmt.bindString(4, entity.getChat_type());
 
        String avatar = entity.getAvatar();
        if (avatar != null) {
            stmt.bindString(5, avatar);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(6, name);
        }
        stmt.bindLong(7, entity.getIs_top());
 
        Long deleted_at = entity.getDeleted_at();
        if (deleted_at != null) {
            stmt.bindLong(8, deleted_at);
        }
 
        Long created_at = entity.getCreated_at();
        if (created_at != null) {
            stmt.bindLong(9, created_at);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ChatRecord readEntity(Cursor cursor, int offset) {
        ChatRecord entity = new ChatRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.getString(offset + 1), // chatroom_id
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // last_open_time
            cursor.getString(offset + 3), // chat_type
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // avatar
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // name
            cursor.getInt(offset + 6), // is_top
            cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7), // deleted_at
            cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8) // created_at
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChatRecord entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setChatroom_id(cursor.getString(offset + 1));
        entity.setLast_open_time(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setChat_type(cursor.getString(offset + 3));
        entity.setAvatar(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setName(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setIs_top(cursor.getInt(offset + 6));
        entity.setDeleted_at(cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7));
        entity.setCreated_at(cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ChatRecord entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ChatRecord entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ChatRecord entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
